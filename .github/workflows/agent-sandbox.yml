name: Agent Sandbox

on:
  workflow_dispatch:
    inputs:
      subfolder:
        description: 'Subfolder to work on'
        required: true
        type: string
      issue_number:
        description: 'Issue number'
        required: true
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  run-sandbox:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Test step
        run: echo "Testing workflow"

name: Agent Sandbox

on:
  workflow_dispatch:
    inputs:
      subfolder:
        description: 'Subfolder to work on'
        required: true
        type: string
      issue_number:
        description: 'Issue number'
        required: true
        type: string

jobs:
  run-sandbox:
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "test"
